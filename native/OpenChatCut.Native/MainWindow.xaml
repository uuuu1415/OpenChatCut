<Window x:Class="OpenChatCut.Native.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OpenChatCut"
        Width="1440"
        Height="900"
        MinWidth="1100"
        MinHeight="700"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Loaded="OnLoaded"
        Closed="OnClosed">
  <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Background="{StaticResource WindowBrush}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="42" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Border Grid.Row="0" Background="#0D121B" MouseLeftButtonDown="OnTitlebarMouseLeftButtonDown">
        <Grid Margin="16,0,8,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
            <Border Width="22" Height="22" CornerRadius="6" Background="{StaticResource CoralBrush}" Margin="0,0,10,0">
              <TextBlock Text="O" Foreground="#10131A" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="OpenChatCut" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
            <TextBlock Text="  NATIVE DESKTOP" Foreground="{StaticResource MutedBrush}" FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" />
          </StackPanel>
          <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="{Binding ConnectionStatus}" Foreground="{StaticResource MutedBrush}" FontSize="11" VerticalAlignment="Center" Margin="0,0,18,0" />
            <Button Content="—" Width="36" Height="28" Background="Transparent" BorderThickness="0" Foreground="{StaticResource MutedBrush}" Click="OnMinimizeClick" />
            <Button Content="□" Width="36" Height="28" Background="Transparent" BorderThickness="0" Foreground="{StaticResource MutedBrush}" Click="OnMaximizeClick" />
            <Button Content="×" Width="36" Height="28" Background="Transparent" BorderThickness="0" Foreground="{StaticResource CoralBrush}" Click="OnCloseClick" />
          </StackPanel>
        </Grid>
      </Border>

      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="216" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,1,0">
          <DockPanel Margin="14,20,14,16">
            <StackPanel DockPanel.Dock="Top">
              <TextBlock Text="工作区" Foreground="{StaticResource MutedBrush}" FontSize="10" FontWeight="SemiBold" Margin="10,0,0,12" />
              <Button Style="{StaticResource NavButton}" Command="{Binding ShowDashboardCommand}">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="⌂" FontSize="17" Width="28" Foreground="{StaticResource CoralBrush}" />
                  <TextBlock Text="工程" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
              </Button>
              <Button Style="{StaticResource NavButton}" Command="{Binding ShowSettingsCommand}">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="⚙" FontSize="15" Width="28" Foreground="{StaticResource BlueBrush}" />
                  <TextBlock Text="设置" FontSize="13" VerticalAlignment="Center" />
                </StackPanel>
              </Button>
            </StackPanel>

            <Border DockPanel.Dock="Bottom" Background="#0D121B" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="10" Padding="12">
              <StackPanel>
                <TextBlock Text="NATIVE RUNTIME" Foreground="{StaticResource GreenBrush}" FontSize="9" FontWeight="Bold" />
                <TextBlock Text="原生窗口 · 本地数据" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,6,0,0" />
                <TextBlock Text="Electron / WebView 未参与绘制" Foreground="{StaticResource MutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,4,0,0" />
              </StackPanel>
            </Border>
          </DockPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentPage}" />
      </Grid>
    </Grid>
  </Border>
</Window>
